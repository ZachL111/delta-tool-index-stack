[project]
name = "delta-tool-index-stack"
version = "0.1.0"
description = "Package a Python local lab for index analysis with capacity fixtures, allocation and spill reports, and documented operating limits."
requires-python = ">=3.11"
